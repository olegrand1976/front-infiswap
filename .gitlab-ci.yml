stages:
  - build
  - deploy

#####################################
# Job de Build
#####################################
#build:
#  stage: build
#  image: docker:20.10.16
#  services:
#    - name: docker:dind
#      alias: docker
#      command: ["--tls=false"]
#  variables:
#    DOCKER_HOST: tcp://docker:2375 
#    DOCKER_TLS_CERTDIR: ""
#    DOCKER_DRIVER: overlay2
#    NODE_ENV: production
#    DOCKER_CLI_NON_INTERACTIVE: "1"
#  cache:
#    key: "$CI_PROJECT_NAME-$CI_COMMIT_REF_SLUG"
#    paths:
#      - node_modules/
#      - .npm/
#  before_script:
#    - apk update
#    - apk add --no-cache nodejs npm
#  script:
#    - if [ -z "$DOCKERHUB_USERNAME" ]; then echo "DOCKERHUB_USERNAME is not set"; exit 1; fi
#    - if [ -z "$DOCKERHUB_PASSWORD" ]; then echo "DOCKERHUB_PASSWORD is not set"; exit 1; fi
#    - rm -rf node_modules
#    - if [ ! -f package-lock.json ]; then npm install --package-lock-only; fi
#    - npm install
#    - export NODE_OPTIONS=--max_old_space_size=8192
#    - npm run build
#    - docker build -t docker.io/$DOCKERHUB_USERNAME/front-app:latest .
#    # Utilisation d'echo -n pour éviter d'ajouter un retour à la ligne
#    - echo -n "$DOCKERHUB_PASSWORD" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
#    - docker push docker.io/$DOCKERHUB_USERNAME/front-app:latest
#    - docker images
#  only:
#    - main

build:
  stage: build
  image: node:20    # Utilise Node 20 pour disposer d'une version récente
  services:
    - name: docker:dind
      alias: docker
      command: ["--tls=false"]
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
    DOCKER_DRIVER: overlay2
    NODE_ENV: production
  cache:
    key: "$CI_PROJECT_NAME-$CI_COMMIT_REF_SLUG"
    paths:
      - node_modules/
      - .npm/
  before_script:
    - apt-get update && apt-get install -y docker.io  # Installer le client Docker sur Debian
  script:
    - if [ -z "$DOCKERHUB_USERNAME" ]; then echo "DOCKERHUB_USERNAME is not set"; exit 1; fi
    - if [ -z "$DOCKERHUB_PASSWORD" ]; then echo "DOCKERHUB_PASSWORD is not set"; exit 1; fi
    - rm -rf node_modules
    - if [ ! -f package-lock.json ]; then npm install --package-lock-only; fi
    - npm install
    - export NODE_OPTIONS=--max_old_space_size=8192
    - npm run build
    - docker build -t docker.io/$DOCKERHUB_USERNAME/front-app:latest .
    - echo -n "$DOCKERHUB_PASSWORD" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
    - docker push docker.io/$DOCKERHUB_USERNAME/front-app:latest
    - docker images
  only:
    - main

#####################################
# Déploiement sur FRONT1
#####################################
deploy_front1:
  stage: deploy
  image: docker:20.10.16
  services:
    - name: docker:dind
      alias: docker
      command: ["--tls=false"]
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
    DOCKER_DRIVER: overlay2
  before_script:
    - apk add --no-cache openssh-client sshpass
    - echo "$SSH_PRIVATE_KEY" > id_rsa
    - chmod 600 id_rsa
    - echo "$SSH_KNOWN_HOSTS" > known_hosts
    # Ajoute la clé SSH de FRONT1 (IP_FRONT_1 doit être défini avec l'adresse IP accessible)
    - if [ -n "$SSH_HOST" ]; then ssh-keyscan -t ed25519 $SSH_HOST >> known_hosts; else echo "SSH_HOST is not set"; fi
  script:
    - docker login -u "$DOCKERHUB_USERNAME" -p "$DOCKERHUB_PASSWORD"
    # Récupération de l'image depuis Docker Hub
    - docker pull docker.io/$DOCKERHUB_USERNAME/front-app:latest
    # Connexion SSH sur FRONT1 pour déployer via docker-compose
    - >
      ssh -i id_rsa -o UserKnownHostsFile=known_hosts -o StrictHostKeyChecking=yes $SSH_USER@$SSH_HOST "
        docker login -u $DOCKERHUB_USERNAME -p $DOCKERHUB_PASSWORD &&
        docker pull docker.io/$DOCKERHUB_USERNAME/front-app:latest &&
        docker compose -f /srv/front/docker-compose.prod.yml down &&
        docker compose -f /srv/front/docker-compose.prod.yml up -d
      "
  only:
    - main