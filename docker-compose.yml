services:
  frontend:
    container_name: infiswap_frontend
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "${FRONT_PORT:-3000}:3000"
    volumes:
      - ".:/infiswap_frontend"
