// Tailwind CSS v4 uses CSS-first configuration with @theme in the CSS file
// This config file is kept for compatibility but most configuration should be in app/assets/css/tailwind.css
// For Tailwind v4, content paths are automatically detected, but we can specify them here if needed

export default {
    content: [
        './app/**/*.{vue,js,ts,jsx,tsx}',
    ],
};
