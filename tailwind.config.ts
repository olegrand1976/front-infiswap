// Tailwind CSS v4 configuration
// In v4, most configuration is done in the CSS file using @theme
// This file is kept minimal for plugin support only

import animate from 'tailwindcss-animate';

export default {
  plugins: [animate],
};
